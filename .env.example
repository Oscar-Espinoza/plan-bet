# Server-only PostgreSQL connection string for the single Neon branch.
DATABASE_URL=

# Server-only token from football-data.org.
FOOTBALL_DATA_API_TOKEN=

# Optional deterministic local/E2E override. Leave unset in production.
MATCHDAY_DATA_MODE=
